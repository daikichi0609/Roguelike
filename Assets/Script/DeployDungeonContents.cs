using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class DeployDungeonContents : SingletonMonoBehaviour<DeployDungeonContents>
{
    [SerializeField] private GameObject m_Stairs;

    public void 
    m_Map = DeployDungeonTerrain.Instance.Map;

        int playerNum = 1;
        while (playerNum != 2)
        {
            m_InitX = Random.Range(0, m_Map.GetLength(0));
            m_InitZ = Random.Range(0, m_Map.GetLength(1));
            playerNum = m_Map[m_InitX, m_InitZ];
        }

Instantiate(m_PlayerObject, new Vector3(m_InitX, 1, m_InitZ), Quaternion.identity);
}
*/